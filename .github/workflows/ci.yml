name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Compile Python
        run: python -m py_compile server/*.py scripts/*.py

      - name: Run API tests
        run: python -m unittest discover -s server -p 'test_*.py' -v

      - name: Check JavaScript syntax
        run: find js -name '*.js' -print0 | xargs -0 -n1 node --check

      - name: Validate static references
        run: python scripts/validate_static.py

      - name: Reject committed runtime secrets
        run: |
          if git grep -nE '(^|[[:space:]])(SMTP_PASSWORD|NOTICE_AI_API_KEY)=.+|BEGIN (RSA |OPENSSH )?PRIVATE KEY' -- \
            ':!*.example' ':!.github/workflows/ci.yml'; then
            echo "Potential secret committed"
            exit 1
          fi
